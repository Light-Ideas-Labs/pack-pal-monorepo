import { RequestHandler } from "express";
declare const imageMulter: RequestHandler;
declare const videoMulter: RequestHandler;
declare const pdfMulter: RequestHandler;
export { imageMulter, videoMulter, pdfMulter };
